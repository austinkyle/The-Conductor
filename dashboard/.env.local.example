NEXT_PUBLIC_GATEWAY_URL=http://localhost:8000
# Optional: bakes a bearer token into the build for a deployed dashboard pointed
# at a token-gated gateway. Leave unset to enter the token via the in-page input
# instead (kept in memory only, never persisted).
NEXT_PUBLIC_DASHBOARD_AUTH_TOKEN=
